# RunWise Web 端 · 工程计划

| 项       | 内容                                                                          |
| -------- | ----------------------------------------------------------------------------- |
| 版本     | v2.0                                                                          |
| 日期     | 2026-08-31                                                                    |
| 状态     | 评审稿                                                                        |
| 范围     | 仅 Web 前端（后端由开发者另行负责，开发期全量 mock）                          |
| 视觉基准 | 已定稿的首页 P5 风格打样图（红黑撞色海报风 / 切角卡片 / 米色胶带 / 跑者剪影） |

> **v2.0 变更（产品定位修正 + 构图改造，定位级重构）**：① **Web 端定位改为「全能训练中枢」**，与小程序「轻量快记录」互补——Web 功能覆盖并超出小程序，删除一切「请前往小程序使用」的引导逻辑；新增 `/profile` 个人中心、`/plan` 训练计划、`/injury` 伤病预防三页（本期建路由 + 页面骨架 + mock 数据源，功能态后续里程碑填充），导航更新为六项。② **构图改造（骨架级）**：新增 3.4「不对称拼贴构图法则」——首页四宫格改 1 大 + 3 小不对称布局（独立旋转 + 垂直错位 + 相邻重叠），Hero 标题允许越界出血 + 整体微倾，打卡页 7:5 / 答疑页 8:4 不等分栏，胶带允许横跨卡片边界，分区斜切分隔；明确「正文与表单控件永不倾斜，只有容器与装饰倾斜」的硬规则。③ **交互修正**：答疑页热门问题 / 历史记录点击改为「填入输入框并聚焦」，禁止自动发送；mock 历史记录去重；月视图折线已按 v1.5 要求完成（回归确认即可）。④ 里程碑重排：新增 **M9 产品定位修正**（A+B）、**M10 构图改造**（C），原 M8 全站视觉精修顺延为皮肤收尾阶段（网点 / 星星 / 水印 / 动画），排在 M9 / M10 全部完成后执行。涉及第 1 / 2.2 / 3.4 / 4 / 5.2 / 7 / 8 章更新。
>
> **v1.5 变更**：① 新增 **M8 全站视觉精修**里程碑——所有非阻塞性样式问题统一推迟至 M8 处理，开发期间不接受逐条样式修改请求；② M6 首页海报化落地——跑者剪影采用自绘 SVG 双影方案（9① 备选路径，不阻塞 M6），新增 GlitchTitle / HeroSection / FeatureGrid 组件；③ 精修四项：标题投影改纯色实心（去半透明残影）、月视图改平滑面积图（隐藏数据点标记）、答疑页聊天区空状态（跑者剪影 + 点击发送预设第一问）、打卡 / 答疑页底部空白压缩。
>
> **v1.4 变更**（UI 精修 v2.0）：① 周统计口径由「自然周周一至今」改为「近 7 天滚动窗口」，图表与汇总卡共用同一数组，卡片数字 = 柱高之和（涉及 5.1 / 5.4 修订）；② P5 浓度补强——三页页头统一「红色斜切竖条 + 斜纹底」，大标题统一红色错位投影，全站大数字统一 Bebas Neue 展示字体，日历打卡标记由圆点改为红色菱形；③ 答疑页热门问题点击由「填入输入框」升级为「直接发起提问」，分类标签去彩色 emoji 改红色菱形前缀；④ 打卡页胶带文案与卡片内容对齐（有记录 → 当日记录）。核心三色（#E60012 / #0A0A0A / #F5F0E6）不变，api 层与 store 层函数签名零改动。
>
> **v1.3 变更**：登录方案定为「账号统一、方式各宜」——小程序保持微信静默登录，Web 用账密登录，凭「小程序内设置账密」绑定同一条用户记录实现双端数据互通；登录契约对齐后端现有 `LoginVO` 双 token 结构（accessToken + refreshToken + device），401 处理升级为「静默刷新优先、失败才踢回」；并核实当前后端 Redis token 为单端覆盖式，双端同时在线需后端加设备维度（列入第 9 章联调依赖清单）。涉及 2.2 / 4.2 / 5.1 / 5.2 / 6.1 / 6.3 / M2 / 第 9 章更新。
>
> **v1.2 变更**：答疑功能定为聊天式 AI 问答（替代 v1.0 的论坛式列表），技术路径采用「分阶段融合」——M4 复用现有 LLM 直连接口起步，后端并行将 RAG 原型（`code/`，LangChain + ChromaDB）服务化接入，接口契约 RAG-ready，切换时前端零改动。涉及 2.2 / 4.5 / 5.1 / 5.2 / 6.3 / M4 / 第 9 章小节更新。

## 目录

1. 项目概述
2. 技术栈与目录结构
3. 设计系统
4. 页面与组件职责
5. Mock 数据方案
6. 接口层设计
7. 路由表
8. 开发里程碑
9. 风险与注意事项

---

## 1. 项目概述

RunWise Web 端是微信小程序「RunWise」的**全能训练中枢**（v2.0 定位修订），面向使用 RunWise 进行跑步训练的普通跑者用户，与小程序共享同一套后端服务与账号体系。双端定位互补：小程序承载**轻量快记录**（扫码即开、每日打卡、随手提问），Web 端凭借大屏与键鼠优势提供**覆盖并超出小程序的功能纵深**——打卡记录的浏览与管理、与「RunWise 助手」的 AI 答疑对话、周/月维度的训练数据可视化、训练计划管理与伤病预防知识库，以及个人中心。视觉上，Web 端采用「女神异闻录（P5）」红黑撞色海报风作为独立设计语言（切角卡片、米色胶带贴纸、跑者剪影、错位投影标题、不对称拼贴构图），以已定稿的首页打样图为基准向全站收敛；后端由开发者自行负责，Web 前端开发期全部使用 mock 数据，并通过统一开关实现与真实接口的无缝切换。

**功能边界说明（v2.0 修订）**：原「训练计划 / 伤痛预防属小程序端功能、Web 仅作入口引导」的边界废除。Web 端本期交付 **8 个页面**：登录 / 首页 / 打卡 / 答疑 / 统计 + 新增训练计划 `/plan`、伤病预防 `/injury`、个人中心 `/profile`（本期为路由 + 骨架 + mock 数据源，功能态在后续里程碑填充）。全站**禁止出现「请前往小程序使用」类引导文案**。

---

## 2. 技术栈与目录结构

### 2.1 技术选型

| 类别 | 选型                                        | 说明                                                                                |
| ---- | ------------------------------------------- | ----------------------------------------------------------------------------------- |
| 框架 | Vue 3（Composition API + `<script setup>`） | 与小程序端（uni-app + Vue3）技术同构，心智复用                                      |
| 构建 | Vite 5                                      | 启动快、env 变量机制成熟（`VITE_*`）                                                |
| 路由 | Vue Router 4（history 模式）                | `meta.requiresAuth` + 全局守卫做访问控制                                            |
| 状态 | Pinia                                       | 仅两个 store：user（登录态）、checkin（打卡数据）                                   |
| 请求 | Axios                                       | 统一封装拦截器，mock 与真实请求共用同一 api 层签名                                  |
| 图表 | ECharts 5（按需引入）                       | 仅柱状图 + 折线图，严格控制体积（见第 9 章）                                        |
| 样式 | 原生 CSS + CSS 变量                         | **不引入任何 UI 框架**：P5 强定制风格下组件库收益为负，视觉还原全靠自研三件套效果类 |
| 语言 | JavaScript                                  | 以给定目录树为基准；第 5 章的 TS 风格结构仅作为数据契约文档，不引入 TS 编译链       |

### 2.2 目录结构（逐文件注释）

```
runwise-web/                          # Web 端工程根目录（后续脚手架产物）
├── index.html                        # Vite 入口 HTML：挂载 #app、自托管字体 preload、深色 meta 主题色
├── vite.config.js                    # Vite 配置：@vitejs/plugin-vue、@ → src 别名、dev 端口、build 产物分析（可选）
├── package.json                      # 依赖声明与脚本：dev / build / preview
├── .env.development                  # 开发环境变量：VITE_API_BASE=/api、VITE_USE_MOCK=true（基准树外补充，见第 6 章）
├── .env.production                   # 生产环境变量：VITE_API_BASE=正式地址、VITE_USE_MOCK=false（联调期临时改 true）
├── docs/
│   └── 工程计划.md                   # 本文档
├── .trae/
│   └── rules/
│       └── project_rules.md          # 项目规则（已配置）：编码规范、提交规范、目录约定
├── public/
│   └── favicon.svg                   # 站点图标：红底斜切「RW」字标
└── src/
    ├── main.js                       # 应用入口：创建实例、注册 Pinia 与 Router、引入三份全局样式
    ├── App.vue                       # 根组件：NavBar 常驻（登录页隐藏）+ router-view
    ├── assets/
    │   ├── fonts/
    │   │   └── bebas-neue.woff2      # Bebas Neue latin 子集，自托管（OFL 开源可商用，约 20KB）
    │   └── images/
    │       ├── runner.png            # 抠好的跑者透明底源图（PNG 兜底格式）
    │       └── runner.webp           # 发布用压缩版：有损 q≈80、宽 ≤800px、目标 <100KB
    ├── styles/
    │   ├── variables.css             # 设计令牌：四色变量 + 扩展色 + 字号 + 间距 + 圆角 + 字体栈 + 层级
    │   ├── base.css                  # modern reset、全局字体、深色底、滚动条与选中文本样式
    │   └── p5-effects.css            # P5 效果工具类：切角卡片 / 胶带贴纸 / 错位投影 / 红色角标 / 噪点纹理
    ├── router/
    │   └── index.js                  # 路由表 + meta 约定 + beforeEach 全局守卫 + afterEach 设标题
    ├── stores/
    │   ├── user.js                   # 登录态：accessToken / refreshToken / userInfo 持久化到 localStorage，login / logout / reset
    │   └── checkin.js                # 打卡数据：按日期索引的记录 Map、当前选中日期、列表加载与提交动作
    ├── api/
    │   ├── request.js                # axios 实例：baseURL / 超时 / 请求拦截带 Bearer accessToken / 响应 401 先静默刷新重放、失败才跳登录（v1.3）
    │   ├── auth.js                   # 账密登录 / 刷新 token / 登出 / 获取用户信息（USE_MOCK 时转发 mock/user）
    │   ├── checkin.js                # 打卡记录增查（USE_MOCK 时转发 mock/checkin）
    │   ├── qa.js                     # AI 问答：提问 / 历史 / 分类 / 热门 / 反馈（USE_MOCK 时转发 mock/qa）
    │   ├── stats.js                  # 周报 / 月报统计（USE_MOCK 时转发 mock/stats）
    │   ├── plan.js                   # 训练计划：4 周计划查询（USE_MOCK 时转发 mock/plan，v2.0 新增）
    │   └── injury.js                 # 伤病预防：知识库文章列表 / 详情（USE_MOCK 时转发 mock/injury，v2.0 新增）
    ├── mock/
    │   ├── index.js                  # USE_MOCK 总开关（读 VITE_USE_MOCK）+ delay 工具 + 各 mock 模块统一出口
    │   ├── user.js                   # 预置账号与登录 / 登出 / 用户信息假实现
    │   ├── checkin.js                # 最近 90 天打卡记录生成器（保证连续天数与统计口径）+ 新增打卡
    │   ├── qa.js                     # AI 问答假实现：预置 FAQ + 模拟 RAG-ready 返回（answer/sources/safetyTip）+ 历史与反馈（v2.0：历史写入时去重）
    │   ├── stats.js                  # 从 checkin mock 实时计算周报 / 月报，保证跨页面数字一致
    │   ├── plan.js                   # 训练计划 mock：固定种子生成 4 周计划（每周 3~5 个课表日： easy / tempo / interval / long / rest），v2.0 新增
    │   └── injury.js                 # 伤病预防 mock：按膝 / 踝 / 髋 / 足四类预置文章（8~12 篇，含标题 / 摘要 / 正文段落 / 标签），v2.0 新增
    ├── composables/
    │   ├── useAuth.js                # 认证行为封装：login（提交 + 写状态 + 跳转）/ logout，供 LoginView 与 NavBar 复用
    │   └── useFormatDate.js          # 纯函数工具：formatDate（YYYY-MM-DD / 中文月日）、formatPace（6'09"/km）、月网格日期生成
    ├── components/
    │   ├── layout/
    │   │   └── NavBar.vue            # 顶部斜切导航：六个入口（v2.0）+ 当前项红色斜块激活态 + 用户昵称 / 退出
    │   ├── common/
    │   │   ├── P5Card.vue            # 切角卡片容器：默认插槽 + 可选胶带文案 + 红色角标开关（v2.0：支持胶带定位覆写与跨边界）
    │   │   ├── TapeTag.vue           # 米色胶带标签：撕口锯齿 + 可配旋转角度与定位
    │   │   └── GlitchTitle.vue       # 错位投影标题：Bebas Neue + 红色错位实体层 + 白描边残影层
    │   └── business/
    │       ├── HeroSection.vue       # 首页 Hero：大标题（越界出血 + 微倾）+ 副标语 + 跑者剪影对角构图 + 赛道弧线（v2.0 构图改造）
    │       ├── FeatureGrid.vue       # 首页入口卡：1 大 + 3 小不对称拼贴（独立旋转 / 错位 / 重叠），全部站内路由（v2.0 重构）
    │       ├── CheckinCalendar.vue   # 打卡日历：月网格 + 打卡日菱形标记 + 选中高亮 + 翻月（自实现，不引第三方日历库）
    │       ├── CheckinForm.vue       # 打卡表单：距离 / 时长 / 自动配速预览 / 心情三选 / 备注 + 校验
    │       ├── QaChatMessage.vue     # 聊天气泡：用户 / 助手双侧样式 + 来源标签 + 安全提示条 + loading 态（v1.2 由 QaListItem 改名重构）
    │       └── StatsChart.vue        # ECharts 封装：按需注册、bar / line 双型（月视图为平滑面积图）、数据 watch 重绘、resize 与 dispose
    └── views/
        ├── LoginView.vue             # 登录页：海报式红黑分屏 + 居中表单卡，已登录访问自动跳走
        ├── HomeView.vue              # 首页：Hero 区 + 不对称入口卡 + 数据条，品牌视觉主阵地
        ├── CheckinView.vue           # 打卡页：日历 + 当日详情 + 打卡表单（7:5 不等分栏，v2.0）
        ├── QaView.vue                # 答疑页：聊天式 AI 问答 + 分类快捷提问 + 热门问题 + 历史记录（8:4 不等分栏，v2.0）
        ├── StatsView.vue             # 统计页：周 / 月切换 + 大小不一的汇总卡 + 图表（v2.0 构图改造）
        ├── PlanView.vue              # 训练计划页（v2.0 新增）：周计划表格视图 + 周切换，本期骨架 + mock
        ├── InjuryView.vue            # 伤病预防页（v2.0 新增）：四分类文章列表 + 文章详情（本期内嵌展开），本期骨架 + mock
        └── ProfileView.vue          # 个人中心（v2.0 新增）：用户资料卡 + 成就徽章 + 本周数据摘要 + 设置 / 退出，本期骨架 + mock
```

> 相对基准目录树的两点补充：新增 `.env.development` / `.env.production`（服务于第 6 章 env 方案）；assets 中明确字体子集文件名与 `runner.webp`（服务于第 9 章压缩方案）。其余结构与基准树一一对应。

---

## 3. 设计系统

### 3.1 设计令牌（`variables.css` 完整清单）

打样图四色体系：**主红**（标题 / 按钮 / 强调）、**主黑**（页面底）、**米色**（胶带纸片）、**白**（正文与大标题）。

**色值冻结（v1.1）**：主红 / 主黑 / 米色自本版起锁定为 `#E60012` / `#0A0A0A` / `#F5F0E6`（打样图取色不可用，按约定回退值锁定），派生色同步微调；**已冻结，后续里程碑禁止再改**。

```css
:root {
  /* ===== 四色体系（核心三色已冻结，后续里程碑禁止再改） ===== */
  --p5-red: #e60012; /* 主红：大标题投影、按钮、角标、激活态。已冻结，禁止再改 */
  --p5-red-dark: #8f000b; /* 深红：红色渐变收尾、hover 压暗（随主红同步微调） */
  --p5-black: #0a0a0a; /* 主黑：页面底色。已冻结，禁止再改 */
  --p5-cream: #f5f0e6; /* 米色：胶带贴纸、纸片元素。已冻结，禁止再改 */
  --p5-white: #ffffff; /* 白：正文、大标题主体 */

  /* ===== 扩展色（由四色派生，仍受变量收敛约束） ===== */
  --p5-panel: #17171a; /* 面板黑：卡片底色（黑上加黑的层次） */
  --p5-red-soft: rgba(230, 0, 18, 0.16); /* 红色蒙层：选中底、悬浮底 */
  --p5-line: rgba(255, 255, 255, 0.14); /* 深色底分隔线 */
  --p5-text-dim: #9c9ca3; /* 次要文字：备注、时间戳 */
  --p5-ink: #2a2a2a; /* 米色纸片上的深色文字 */
  --p5-shadow: rgba(0, 0, 0, 0.55); /* 卡片投影 */

  /* ===== 字号 ===== */
  --fs-hero: clamp(64px, 9vw, 120px); /* 首页 RUN WISE */
  --fs-data: 56px; /* 统计大数字 */
  --fs-h1: 40px;
  --fs-h2: 28px;
  --fs-h3: 20px;
  --fs-body: 16px;
  --fs-sub: 14px;
  --fs-caption: 12px;

  /* ===== 间距 ===== */
  --sp-1: 4px;
  --sp-2: 8px;
  --sp-3: 12px;
  --sp-4: 16px;
  --sp-5: 24px;
  --sp-6: 32px;
  --sp-7: 48px;
  --sp-8: 64px;

  /* ===== 圆角（整体走硬朗刀切风，仅微小元素用圆角） ===== */
  --radius-none: 0; /* 卡片默认直角（形态由切角定义） */
  --radius-tag: 2px; /* 小标签 */
  --radius-pill: 999px; /* 胶囊按钮 / tab */

  /* ===== 字体栈 ===== */
  --font-display:
    "Bebas Neue", "Impact", "Arial Narrow", sans-serif; /* 英文大标题与数字 */
  --font-body: "Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif;

  /* ===== 层级 ===== */
  --z-nav: 100;
  --z-hero: 10;
  --z-tape: 20; /* 胶带永远压在卡片之上 */

  /* ===== 布局 ===== */
  --page-max: 1200px;
  --nav-h: 64px;
  --cut: 18px; /* 全站统一切角尺寸 */
}
```

**字体方案**：Bebas Neue（OFL 开源可商用）只用于英文大标题与数字（RUN WISE / 28.6 / 7），自托管 latin 子集并 `font-display: swap`；中文走系统黑体栈，标题用 900 字重 + 收紧字距营造海报感；所有数字加 `font-variant-numeric: tabular-nums` 保证对齐。

### 3.2 三件套效果实现方案

**① 切角卡片（P5Card 核心）**

```css
/* 全站统一的双对角斜切：右上 + 左下各切 --cut（18px） */
.p5-card {
  position: relative;
  background: var(--p5-panel);
  clip-path: polygon(
    0 0,
    calc(100% - var(--cut)) 0,
    100% var(--cut),
    100% 100%,
    var(--cut) 100%,
    0 calc(100% - var(--cut))
  );
}

/* clip-path 会裁掉 box-shadow，投影挂到外层容器用 drop-shadow */
.has-card-shadow {
  filter: drop-shadow(0 6px 16px var(--p5-shadow));
}

/* 右下红色三角角标：打样图四宫格的标志性收尾 */
.p5-card::after {
  content: "";
  position: absolute;
  right: 0;
  bottom: 0;
  width: 34px;
  height: 34px;
  background: var(--p5-red);
  clip-path: polygon(100% 0, 100% 100%, 0 100%);
}
```

**② 胶带贴纸（TapeTag 核心）**

```css
/* 米色半透明纸质感 + 斜贴 + 单元素 clip-path 做两端撕口锯齿 */
.tape {
  position: absolute;
  top: -12px;
  left: -16px;
  z-index: var(--z-tape);
  padding: 5px 20px;
  font-size: var(--fs-caption);
  color: var(--p5-ink);
  background: linear-gradient(
    100deg,
    rgba(245, 240, 230, 0.95),
    rgba(228, 220, 200, 0.82)
  );
  transform: rotate(-6deg); /* 角度可按实例覆写 */
  box-shadow: 0 3px 8px rgba(0, 0, 0, 0.35);
  clip-path: polygon(
    4% 0,
    96% 5%,
    100% 28%,
    97% 52%,
    100% 78%,
    95% 100%,
    6% 97%,
    0 76%,
    3% 48%,
    0 20%
  );
}
```

**③ 错位投影标题（GlitchTitle 核心）**

```css
/* 白色主体 + 红色错位实体层（左上） + 白描边残影层（右下） */
.glitch {
  position: relative;
  isolation: isolate; /* 建立独立层叠上下文：防止 ::before 的 z-index:-1 沉入容器背景之后 */
  font-family: var(--font-display);
  color: var(--p5-white);
  letter-spacing: 0.03em;
}
.glitch::before,
.glitch::after {
  content: attr(data-text); /* 文案需同时写入元素文本与 data-text 属性 */
  position: absolute;
  left: 0;
  top: 0;
  pointer-events: none;
}
.glitch::before {
  color: var(--p5-red);
  transform: translate(7px, -5px);
  z-index: -1;
}
.glitch::after {
  color: transparent;
  -webkit-text-stroke: 2px rgba(255, 255, 255, 0.4);
  transform: translate(-5px, 4px);
}
```

三件套全部沉淀为 `p5-effects.css` 的工具类，组件只挂类名，禁止在业务组件里重复实现效果。

### 3.3 风格浓度分级表

P5 海报风的克制比放开更难。全站按五个浓度档执行，胶带使用数量是硬约束：

| 浓度档 | 页面 / 元素                | 视觉强度 | 边界说明                                                                 |
| ------ | -------------------------- | -------- | ------------------------------------------------------------------------ |
| 全开   | 首页 Hero 区               | ★★★★★    | 错位投影大标题、跑者剪影、赛道弧线、红色几何飞溅全部放开                 |
| 放得开 | 登录页、首页四宫格         | ★★★★     | 切角 + 胶带 + 红色角标齐全，胶带允许大角度（-8° 至 -4°）                 |
| 适中   | NavBar、数据条、统计页     | ★★★      | 结构清晰优先：红黑配色 + 局部切角，胶带 ≤2 处，图表内部不用效果          |
| 克制   | 打卡页、答疑页             | ★★       | 内容可读性优先：仅容器切角与小型 TapeTag（分类标签），禁止大面积红色几何 |
| 极简   | 表单控件、表格、图表坐标区 | ★        | 标准输入框与坐标轴，仅主色点缀选中态 / 激活态                            |

### 3.4 不对称拼贴构图法则（v2.0 新增，M10 执行基准）

**诊断**：当前页面元素不少但排布过于对称均匀（等宽栅格 + 等高卡片 + 胶带统一贴左上），呈现「AI 味的工整」而非「P5 的拼贴张力」。本节给出骨架级构图规则，目标是把「丑的乱」变成「酷的丰富」——乱得有秩序：**倾斜、错位、重叠三件套制造张力，网格与对齐基线兜住可读性**。

**3.4.1 倾斜令牌（收敛进 `variables.css`，禁止业务代码写死角度）**

```css
:root {
  --tilt-1: -3deg; /* 大卡 / Hero 标题 */
  --tilt-2: 2deg; /* 小卡 A */
  --tilt-3: -1.5deg; /* 小卡 B */
  --tilt-4: 3deg; /* 小卡 C / 胶带 */
  --tilt-hero: -2deg; /* Hero 文案区整体 */
}
```

**3.4.2 五条骨架规则**

1. **不对称栅格**：禁止所有内容页使用均分栅格。首页入口区 = 1 张大卡（今日打卡，占两列行高）+ 3 张小卡；打卡页双栏改 **7:5**；答疑页改 **8:4**；统计页三张汇总卡改「大 / 中 / 小」三档宽度 + 各自微倾（--tilt-1~3）+ 垂直错位 8~12px。
2. **独立旋转与错位**：入口卡片每张独立旋转（-3° / +2° / -1.5° / +3°）+ 相邻卡垂直错位 8~12px；相邻卡片允许**重叠 10~20px**（z-index 依视觉层次递增），重叠只发生在卡片**边缘留白区**，禁止压住对方文字与可点击区域。
3. **越界与出血**：Hero 大标题允许溢出容器右边界（`white-space: nowrap` + 负 margin 出血），Hero 文案区整体微倾 -2°；跑者剪影与文字形成**对角线构图**（文字左上 ↔ 剪影右下）。允许装饰元素（弧线 / 星形 / 斜切块）被容器 `overflow: hidden` 裁切的出血画法。
4. **胶带自由化**：胶带允许**横跨卡片边界**（top 为负值、可贴右上 / 右下 / 居中），旋转角度在 -8° ~ +6° 内取值，同一页面胶带的贴法不得重复；P5Card 新增 `tag-position` 覆写能力。
5. **斜切分隔**：区块之间用 clip-path 对角线分隔（红 / 黑交错的斜切块，角度与 --cut 同族 ±12°），替代水平 border 分割线；页头与内容区之间必用斜切分隔。

**3.4.3 硬规则（防「乱」失控）**

- **正文文字与表单控件永不倾斜**：`input / textarea / button 正文 / 表格 / 图表坐标区` 一律水平，只有容器与装饰元素倾斜；
- 重叠卡片的 hover 态 = `translateY(-4px) + z-index 提升 + 投影加深`，hover 后必须完整露出被压边缘，保证可点性；
- 旋转与错位只作用于 ≥ 卡片级容器，页面级滚动方向上不得产生横向溢出（出界元素一律被 `overflow: hidden` 的舞台容器裁切）；
- 每页「倾斜元素数量」上限 = 该页浓度档胶带数 × 2，防止全页歪斜。

---

## 4. 页面与组件职责

### 4.1 全局组件

**NavBar.vue（layout）**

- 职责：顶部斜切黑色导航条，右侧入口以红色斜块作为激活态；展示当前用户昵称与退出入口。
- 菜单与映射（v2.0 六项）：首页 `/home`、打卡记录 `/checkin`、训练计划 `/plan`、AI 答疑 `/qa`、伤病预防 `/injury`、个人中心 `/profile`。
- 说明：v2.0 起导航收敛规则废止——训练计划 / 伤病预防已有站内路由，导航按六项实际路由展示；「数据统计」从导航移除，入口保留在首页数据条与个人中心（统计属低频回访场景，导航让位给高频功能页）。
- 数据来源：`stores/user`（昵称、登出动作）。

**P5Card.vue（common）**

- 职责：切角卡片容器；props：`tag`（胶带文案，可选，有值时内部渲染 TapeTag）、`accent`（右下红色三角角标开关，默认开）；默认插槽承载内容。

**TapeTag.vue（common）**

- 职责：米色胶带标签；props：`text`、`rotate`（默认 -6deg）；默认绝对定位于父容器左上，允许通过样式覆写位置。

**GlitchTitle.vue（common）**

- 职责：错位投影标题；props：`text`、`level`（h1/h2）；内部保证元素文本与 `data-text` 属性同步。

### 4.2 LoginView（/login）

- 使用组件：GlitchTitle、P5Card、TapeTag；隐藏 NavBar。
- 数据来源：`api/auth.login`（mock，契约对齐后端 `LoginVO` 双 token，见 5.1）；经 `composables/useAuth` 写入 `stores/user`（accessToken / refreshToken / userInfo）。
- 关键交互：
  - 用户名 / 密码非空校验，回车提交；
  - 提交后按钮 loading（mock 延迟 400ms），成功后跳 `query.redirect` 或 `/home`；
  - 已登录访问本页自动跳 `/home`（守卫负责）；
  - 登录失败区分提示：账密错误提示重试；联调后若后端返回「无账密」类错误，引导「请先在微信小程序『我的 → 设置账密』中绑定账号」；
  - 页面本身海报化：左半红色斜切块 + GlitchTitle 标语，右侧居中登录卡。

### 4.3 HomeView（/home）【v2.0 构图改造】

- 使用组件：HeroSection、FeatureGrid；数据条直接在页面内实现（三个 Bebas Neue 大数字，复用 GlitchTitle / P5Card）。
- 数据来源：数据条三项来自 `api/stats.getWeeklyStats`（mock），经 checkin mock 单一数据源计算，与统计页数字必然一致。
- 关键交互：
  - HeroSection 纯展示，无业务交互；构图执行 3.4.2 规则 3（标题越界出血 + 微倾 + 对角线构图）；
  - 入口卡（不对称 1+3，执行 3.4.2 规则 1 / 2）：**今日打卡（大卡）→ /checkin**、**智能问答 → /qa**、**训练计划 → /plan**、**伤病预防 → /injury**（v2.0 起全部站内路由，「跳转小程序」逻辑与文案全站删除）；
  - 数据条三个数字可点击，跳转 `/stats`。

### 4.4 CheckinView（/checkin）【v2.0 构图：7:5 不等分栏】

- 使用组件：CheckinCalendar、CheckinForm（外层套 P5Card）；双栏栅格改 `7fr 5fr`（v2.0），胶带贴法执行 3.4.2 规则 4。
- 数据来源：`stores/checkin` → `api/checkin`（mock）；选中日期状态收在 store。
- 关键交互：
  - 日历默认展示当月，可前后翻月；有打卡的日期渲染红点，选中日期高亮；
  - 选中日期下方展示当天完整记录（距离 / 时长 / 配速 / 心情 / 备注）；
  - 打卡日期固定为「今天」，不可选未来日期；当天已打卡时表单转为只读状态卡并展示当日记录；
  - 提交合法表单 → store 更新 → 红点与详情即时刷新，无需整页刷新；
  - 配速由 `formatPace(distanceKm, durationMin)` 随输入实时预览。

### 4.5 QaView（/qa）【v1.2 重构：聊天式 AI 问答】

- **技术路径（分阶段融合）**：M4 复用现有后端 `POST /api/qa/ask`（LLM 直连）起步；后端并行将 `code/` 下的 RAG 原型（LangChain + ChromaDB + bge-small-zh）服务化，在 `QaServiceImpl.ask()` 内部增加「先检索后生成」分支，异常自动降级直连。ask 返回结构 `{answer, sources, safetyTip}` 天然 RAG-ready，**后端切换检索增强时前端零改动**；SSE 流式输出为后续增强，首版用 loading 态。
- 使用组件：QaChatMessage、TapeTag（分类快捷提问标签）。
- 数据来源：直接调用 `api/qa`（mock）；会话消息为组件局部状态，**不建 store**，历史记录经 `getHistory` 分页拉取。
- 关键交互：
  - 聊天流布局：用户消息右侧红底气泡、助手回复左侧深色切角气泡，自动滚动到底部；
  - 发送提问：输入框回车 / 按钮提交 → 用户气泡立即上屏 → 助手气泡进入 loading（三点动画，mock 延迟 800ms）→ 渲染 `answer`；
  - 来源与安全提示：助手气泡底部渲染 `sources` 来源标签（直连阶段显示模型标识，RAG 阶段显示真实命中文档名）；`safetyTip` 存在时在气泡下方渲染米色提示条（mock 对含「疼 / 痛 / 伤 / 膝盖」等关键词的提问模拟返回）；
  - 分类快捷提问（v2.0 重设计，禁止复读机式发送）：四分类 chips（`训练计划 / 装备选择 / 伤痛预防 / 跑步技术`，来自 `getCategories`）点击**不发送也不直接填入**，而是展开该分类的二级候选条（3~4 条子话题问题）；其中**第一条为「数据注入」的个性化问题**——前端用 `getWeeklyStats` 实时组装（如「我这周跑了 18.5km、打卡 3 次，下周课表怎么安排？」），保证同一 chip 不同时间点出的问题不同、AI 回答对症；点击候选问题 → 填入输入框并聚焦，用户编辑确认后发送；再次点击 chip 收起候选条。
  - 热门问题：侧栏展示 `getHotQuestions` 列表，**点击将问题文本填入输入框并聚焦光标，由用户确认后再发送（v2.0 修订：禁止自动发送，避免「一点就自问自答」的失控感）**；
  - 历史记录：进入页面拉取最近问答（`getHistory` 分页），支持「加载更多」回溯；**点击历史条目同样改为填入该问题草稿并聚焦（v2.0 修订）**；mock 历史集写入时按「日期 + 问题」去重（v2.0）；
  - 反馈：每条助手回复下方 👍 / 👎，调用 `feedback` 落库，切换即更新，不弹确认；
  - 空态 / 异常：无历史时展示空状态面板（跑者剪影 + 引导文案），**点击空状态仅将预设问题填入输入框并聚焦，不自动发送（v2.0 修订）**；ask 失败时在聊天流中插入错误气泡并提供「重试」按钮，不允许静默失败。

### 4.6 StatsView（/stats）【v2.0 构图改造】

- 使用组件：StatsChart（周柱状 / 月平滑面积图两实例或同实例重绘）。
- 构图（v2.0）：顶部三张汇总卡改「大 / 中 / 小」三档宽度 + 各自微倾（--tilt-1~3）+ 垂直错位 8~12px（执行 3.4.2 规则 1 / 2）；数据条与图表卡之间用斜切分隔（规则 5）。
- 数据来源：`api/stats.getWeeklyStats` 与 `api/stats.getMonthlyStats`（mock）。
- 关键交互：
  - 周 / 月分段切换：周视图 = 本周一至周日 7 天里程柱状图；月视图 = 当月逐日趋势折线图；
  - 顶部汇总（总里程 / 打卡次数 / 连续天数）随口径联动，与图表同源；
  - 图表 hover 显示完整数值 tooltip；窗口 resize 自适应。

### 4.7 PlanView（/plan）【v2.0 新增，本期骨架 + mock】

- 职责：训练计划中枢——以**周计划表格视图**展示 4 周课表（周切换），与打卡数据联动展示「计划 vs 实际」完成度（本期先做视图与 mock，联动在功能态里程碑填充）。
- 数据来源：`api/plan.getPlanList()` → `mock/plan.js`（固定种子生成 4 周计划；每周 3~5 个课表日，类型 `easy / tempo / interval / long / rest`，含目标距离与配速区间）。
- 关键交互：
  - 周切换（第 1~4 周 tab 或前后翻页），当前周高亮；
  - 表格行 = 课表日：星期 / 类型标签 / 目标距离 / 目标配速 / 状态（本期全部「待执行」占位）；
  - 构图执行 3.4：页头斜切分隔 + 胶带自由贴法。
- 后续里程碑：课表点击 → 跳打卡页预填；按实际打卡自动勾选完成状态；AI 生成个性化计划（依赖后端）。

### 4.8 InjuryView（/injury）【v2.0 新增，本期骨架 + mock】

- 职责：伤病预防知识库——按**膝 / 踝 / 髋 / 足**四分类的文章列表 + 文章详情阅读。
- 数据来源：`api/injury.getArticles(category)` / `getArticleDetail(id)` → `mock/injury.js`（8~12 篇预置文章：标题 / 摘要 / 正文段落数组 / 标签 / 分类）。
- 关键交互：
  - 四分类 chips 切换文章列表（列表卡片 = 切角卡 + 胶带分类标签）；
  - 点击文章 → **本页内嵌展开详情**（路由 query 带 `?article=id`，刷新可定位），返回列表保留滚动位置；
  - 构图执行 3.4：列表卡片错位排布，禁止整齐三栏。
- 后续里程碑：与答疑页联动（文章底部「问 AI 助手」携带上下文跳转 /qa）；收藏功能。

### 4.9 ProfileView（/profile）【v2.0 新增，本期骨架 + mock】

- 职责：个人中心——用户资料卡、成就徽章、本周数据摘要、设置与退出。
- 数据来源：资料与 joinedDays 来自 `stores/user`（`fetchProfile`，joinedDays mock 固定 128）；成就徽章与本周摘要本期用 mock / `api/stats.getWeeklyStats`。
- 页面结构（自上而下）：
  1. **资料卡**：头像 + 昵称 + 「已坚持 N 天」（Bebas 大数字）+ 加入日期；
  2. **成就徽章区**：徽章网格（已达成 = 红色实心菱形章 + 名称；未达成 = 灰描边虚章），本期预置 6 枚（首次打卡 / 连续 7 天 / 累计 100km / 周末跑者 / 夜跑侠 / 全勤月）达成状态取自打卡 mock 推导（本期可静态占位）;
  3. **本周数据摘要**：三数字（里程 / 次数 / 连续），点击跳 `/stats`；
  4. **设置区**：退出登录（复用 `useAuth.logout`）；其余设置项（消息通知 / 深色模式等）本期灰色占位「开发中」。
- 构图执行 3.4：资料卡横贯全宽 + 徽章区不对称网格 + 胶带跨边界。

---

## 5. Mock 数据方案

### 5.1 核心结构定义（前后端数据契约，后端字段命名须与此对齐）

```ts
UserInfo {
  id: string
  username: string
  nickname: string
  avatar: string
  joinedDays: number      // 已加入天数
}

CheckinRecord {
  id: string
  date: 'YYYY-MM-DD'
  distanceKm: number      // 如 5.2
  durationMin: number     // 如 32
  pace: string            // 如 "6'09\"/km"
  mood: 'great' | 'good' | 'tired'
  note: string
}

QaAskResult {
  answer: string            // AI 回答正文（200-400 字）
  sources: string[]         // 来源标注：LLM 直连阶段为模型标识（如 ['Qwen3.5-Flash大模型生成']），RAG 阶段为命中文档名
  safetyTip?: string        // 命中伤病关键词时附加的就医安全提示
}

QaHistoryRecord {
  id: string
  question: string          // 用户提问
  answer: string            // AI 回答
  sources: string           // 来源（后端落库为字符串，前端 JSON.parse 后渲染，容错处理空值）
  feedback: number          // 0 未反馈 / 1 赞 / -1 踩
  createTime: string        // 'YYYY-MM-DD HH:mm:ss'
}

QaCategory {
  name: string              // 如 '训练计划'
  icon: string              // emoji，如 '📋'
}

HotQuestion {
  question: string
  category: string          // 如 'injury'
}

WeeklyStats {
  weekDates: string[]     // 近 7 天滚动窗口日期（今天−6 至今天，v1.4 修订）
  distances: number[]     // 对应 7 天里程（与汇总卡同源，卡片数字 = 柱高之和）
  totalKm: number
  checkinCount: number
  streakDays: number      // 连续天数
}
```

补充结构（里程碑 M2 / M5 需要）：

```ts
LoginResult {              // v1.3：直接对齐后端现有 LoginVO（AuthController 实际返回结构）
  accessToken: string      // 短效凭证（后端 JWT 2h），mock 生成 'mock-access-' + 时间戳
  refreshToken: string     // 续期凭证（后端 JWT 7天），mock 生成 'mock-refresh-' + 时间戳
  userId: string           // 双端互通的主键标识（后端 Long，前端按字符串处理）
  nickname: string
  avatarUrl: string
  isNewUser: boolean       // 首登标记，Web 端仅用于欢迎提示
}

MonthlyStats {
  monthDates: string[]    // 当月 1 日至月末逐日
  distances: number[]     // 对应里程，无打卡为 0
  totalKm: number
  checkinCount: number
}
```

> **登录契约与双端互通（v1.3）**：双端方案为「账号统一、方式各宜」——小程序保持微信静默登录（openid 体系），Web 用账密登录，用户在小程序「我的 → 设置账密」后，`openid` 与 `username` 共存于同一条 User 记录，Web 登录即同一 `userId`，打卡 / 答疑 / 统计数据天然互通。前端 `UserInfo` 的字段映射（在 `stores/user.js` 一处完成）：`id ← userId`、`avatar ← avatarUrl`、`username ← 账密登录名（可能为空，NavBar 一律显示 nickname）`、`joinedDays ← 前端按用户创建时间计算（mock 固定 128）`。

### 5.2 各 mock 文件职责

| 文件              | 职责                                                                                                                                                                                                                                                      |
| ----------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `mock/user.js`    | 预置统一用户「跑者小王」（joinedDays = 128）；任意非空账密均可登录，`login` 延迟 400ms 返回对齐 `LoginVO` 的双 token 结构（accessToken / refreshToken / userId / nickname / avatarUrl / isNewUser）；提供 `refreshToken` / `getProfile` / `logout` 假实现 |
| `mock/checkin.js` | 最近 90 天打卡记录生成器 + `createCheckin` 写入内存记录集；是全部统计数字的**单一数据源**                                                                                                                                                                 |
| `mock/qa.js`      | AI 问答假实现：`ask` 延迟 800ms 返回 QaAskResult（含伤病关键词 → safetyTip 的模拟逻辑）；内存历史记录集（预置 8 条）支持分页与反馈；提供分类 / 热门假数据。**v2.0：历史写入时按「日期 + 问题」去重，预置数据亦去重，杜绝同条记录重复展示**                |
| `mock/stats.js`   | `getWeeklyStats` / `getMonthlyStats` 均从 checkin 记录**实时计算**，不另造数据，保证首页数据条、打卡页、统计页三处数字一致                                                                                                                                |
| `mock/plan.js`    | **v2.0 新增**：固定种子生成 4 周训练计划（每周 3~5 课表日，类型 easy / tempo / interval / long / rest，含目标距离与配速区间），`getPlanList()` 返回完整 4 周                                                                                              |
| `mock/injury.js`  | **v2.0 新增**：按膝 / 踝 / 髋 / 足四类预置 8~12 篇文章（标题 / 摘要 / 正文段落 / 标签），`getArticles(category)` / `getArticleDetail(id)`                                                                                                                 |
| `mock/index.js`   | 导出 `USE_MOCK`（读 `import.meta.env.VITE_USE_MOCK === 'true'`，缺省 true）与 `delay(ms)` 工具                                                                                                                                                            |

### 5.3 checkin.js 的 90 天生成算法（保证连续天数逻辑可算出）

以「运行当天」为锚点，生成 [今天 − 89 天， 今天] 共 90 天：

1. **随机断点**：以固定种子从 {今天 − 5、今天 − 6、今天 − 7} 中随机取一天设为**强制休息日 R**（避免连续天数与滚动打卡数恒定不变）；
2. **连续段**：R 的次日（R + 1）至今天**强制打卡** → `streakDays` 为 5~7 的浮动值（断点越靠近今天，数值越小）；
3. 本周一至今天中，除 R 外**同样强制打卡** → 本周除休息日外全勤（若 R 落在本周，自然周打卡次数 = 已过天数 − 1，否则 = 已过天数）；
4. 其余日期以 55% 概率随机打卡（与上述规则取并集，不冲突）；
5. **固定随机种子**：以 `'runwise-' + 日期字符串` 做 hash 种子 → 同一天多次刷新数据完全一致，演示稳定；
6. 字段随机规则：`distanceKm` 取 3.0~10.0（一位小数）；`durationMin` = round(distanceKm × paceMin)，paceMin 在 5.2~7.5 随机；`pace` 由 `formatPace` 推导；`mood` 加权随机（great 20% / good 55% / tired 25%）；30% 的记录带 `note`（从 8 条预设文案池取）。

**下界保证**：连续段（≥ 5 天）整体位于近 7 天滚动窗口内，故任意一天访问均有 `checkinCount ≥ streakDays ≥ 5 > 4`，滚动打卡数不再恒定。

### 5.4 统计口径说明（重要）

- `streakDays`：以今天为终点的连续打卡天数，由随机断点决定，为 5~7 的浮动值（固定种子保证同一天内稳定）。
- 周口径统一为**近 7 天滚动窗口**（今天−6 至今天，v1.4 修订，原为自然周）：`weekDates` / `distances` / `checkinCount` / `totalKm` 共用同一数组计算，图表柱高之和 = 汇总卡数字，任意一天访问 7 根柱齐；「本周 ≥4 次」在周一、周二物理上不可能成立（已过天数不足 4 天），滚动口径下连续段 ≥5 天整体位于窗口内，恒有 `checkinCount ≥ 5`。真实接口联调时与后端约定同口径。
- 打样图上的 28.6km / 4 次 / 7 天为视觉占位数字，实际展示以 mock 计算结果为准，不追求与图上数字一一对应。

---

## 6. 接口层设计

### 6.1 request.js（axios 封装）【v1.3：静默刷新优先】

- `baseURL` 读 `import.meta.env.VITE_API_BASE`，缺省 `/api`；`timeout` 10s。
- **请求拦截器**：从 `stores/user`（兜底 localStorage）读 **accessToken**，自动追加 `Authorization: Bearer <accessToken>`；无 token 不带该头。
- **响应拦截器（401 处理：先刷新、失败才踢回）**——移植小程序 `request.ts` 已验证的刷新队列模式：
  1. 收到 HTTP 401 且非 `/auth/refresh` 自身请求 → 用 refreshToken 调 `auth.refreshToken()`；
  2. **并发排队**：刷新进行中时，后续 401 请求挂起等待，刷新成功后携带新 accessToken **重放原请求**（不丢请求、不重复刷新）；
  3. 刷新成功 → 更新 store 中双 token，队列中的挂起请求依次重放；
  4. 刷新失败（refreshToken 过期 / 后端拒绝）→ `userStore.reset()` 清双 token → 跳 `/login?redirect=当前 fullPath` → 统一提示「登录已过期」。
- **其余响应处理**：业务码非成功 → reject 并统一 toast（约定 `res.data.code === 200` 为成功，联调期与后端核对）；网络错误 → 统一错误提示，reject 透传。
- 开发期 mock 分支不经过 axios：双 token 存取与刷新队列在 M2 通过「手动篡改 localStorage 中的 accessToken 模拟过期」验证，真实生效在联调期。

### 6.2 mock 开关（组件层无感知）

```
VITE_USE_MOCK = true | false
```

- `mock/index.js` 导出常量 `USE_MOCK`；
- 每个 api 函数内部第一行分支：`USE_MOCK` 为真 → 返回对应 mock 函数（带 delay 模拟网络）；为假 → 走 axios 实例；
- 组件与 store 只 import api 函数，**永远不感知数据来源**；切换环境只改 env 文件，零改业务代码。

### 6.3 api 函数签名清单

```text
api/auth.js
  login(username, password)                    → Promise<LoginResult>       // 联调期对应后端新增 POST /api/auth/login-password，请求体附 device: 'web'
  refreshToken()                               → Promise<LoginResult>       // 对应现有 POST /api/auth/refresh；仅 request.js 拦截器内部调用，业务代码不直接使用
  logout()                                     → Promise<void>              // 对应 POST /api/auth/logout
  getProfile()                                 → Promise<UserInfo>

api/checkin.js
  getCheckinList(startDate, endDate)           → Promise<CheckinRecord[]>   // 'YYYY-MM-DD' 闭区间
  getCheckinByDate(date)                       → Promise<CheckinRecord | null>
  createCheckin({ date, distanceKm, durationMin, mood, note })             → Promise<CheckinRecord>  // pace 由 api 层推导写入

api/qa.js
  askQuestion(question)                        → Promise<QaAskResult>       // 对应后端 POST /api/qa/ask（AskDTO.question）
  getHistory(pageNum = 1, pageSize = 10)       → Promise<{ list: QaHistoryRecord[], total: number }>
  getCategories()                              → Promise<QaCategory[]>
  getHotQuestions(limit = 5)                   → Promise<HotQuestion[]>
  feedback(id, feedback)                       → Promise<void>              // feedback: 1 赞 / -1 踩，对应 POST /api/qa/feedback

api/stats.js
  getWeeklyStats()                             → Promise<WeeklyStats>
  getMonthlyStats(month)                       → Promise<MonthlyStats>      // month: 'YYYY-MM'，缺省当月

api/plan.js                                   // v2.0 新增（本期 mock，联调期与后端对齐路径）
  getPlanList()                                → Promise<PlanWeek[]>        // 4 周计划，PlanWeek { weekNo, days: PlanDay[] }
                                                                            // PlanDay { weekday, type: 'easy'|'tempo'|'interval'|'long'|'rest', targetKm, paceRange, status }

api/injury.js                                 // v2.0 新增（本期 mock）
  getArticles(category)                        → Promise<InjuryArticle[]>   // category: 'knee'|'ankle'|'hip'|'foot'，缺省全部
  getArticleDetail(id)                         → Promise<InjuryArticle>     // 含 contentParagraphs: string[]
```

---

## 7. 路由表

| 路径     | 页面        | 是否需要登录 | 备注                         |
| -------- | ----------- | ------------ | ---------------------------- |
| /login   | LoginView   | 否           | 已登录访问则跳 /home         |
| /home    | HomeView    | 是           | 默认重定向                   |
| /checkin | CheckinView | 是           |                              |
| /plan    | PlanView    | 是           | v2.0 新增                    |
| /qa      | QaView      | 是           |                              |
| /injury  | InjuryView  | 是           | v2.0 新增，query 定位文章    |
| /profile | ProfileView | 是           | v2.0 新增                    |
| /stats   | StatsView   | 是           | 导航不展示（v2.0），路由保留 |

补充约定：

- `/` 路由 `redirect: '/home'`，未登录时被守卫规则接住，自然落到 `/login`；
- 每条路由带 `meta: { requiresAuth, title }`，`title` 用于 `afterEach` 设置 `document.title`（格式「页面名 · RunWise」）；
- 全局 `scrollBehavior` 统一回到顶部。

### router.beforeEach 全局守卫判断逻辑

1. 读取 `userStore.isLoggedIn()`：开发期只判 token 存在性（localStorage 有值即视为已登录，不校验过期）；
2. 若 `to.meta.requiresAuth !== false` 且未登录 → `next({ path: '/login', query: { redirect: to.fullPath } })`，登录成功后回跳原目标；
3. 若 `to.path === '/login'` 且已登录 → `next('/home')`；
4. 其余情况一律放行；
5. 401 兜底与守卫共用同一个 `userStore.reset()`，保证「响应拦截踢出」与「守卫拦截」状态一致，不会出现半登录态。

---

## 8. 开发里程碑

> 顺序执行，每个里程碑可独立验收；M3 起功能页先用「默认 P5Card + 从简样式」跑通数据流，视觉统一集中在 M6 / M7，避免功能与视觉互相阻塞。

### M1 项目骨架

**目标**：建立可运行的空壳工程与全局设计基座（变量、效果类、P5Card 切角卡片组件、导航、路由）。

**新建文件清单**：`package.json`、`vite.config.js`、`index.html`、`.env.development`、`public/favicon.svg`、`src/main.js`、`src/App.vue`、`src/styles/variables.css`、`src/styles/base.css`、`src/styles/p5-effects.css`（先收切角卡片一类）、`src/router/index.js`、`src/components/layout/NavBar.vue`、`src/components/common/P5Card.vue`、`src/views/` 下 5 个占位视图。

**验收标准**：

- [ ] `npm install && npm run dev` 正常启动，控制台无报错
- [ ] 手动访问 /login /home /checkin /qa /stats 五个路由均渲染对应占位标题
- [ ] NavBar 可见且四个入口点击切换正确，当前项有红色斜块激活态
- [ ] `/` 自动重定向到 `/home`
- [ ] 占位页已使用 variables.css 变量（临时改一处主色变量，全页颜色联动可验证）

**预计产出**：红黑底色、可导航的空站；NavBar 呈现斜切形态；后续所有页面共享统一的令牌与布局骨架。

### M2 登录闭环【v1.3：契约对齐 LoginVO + 静默刷新】

**目标**：打通认证链路与路由访问控制。

**说明**：① 本阶段 LoginView 仅实现功能与基础布局（表单 + 基础红黑配色），第 4.2 节描述的海报化视觉（红色斜切分屏、GlitchTitle 标语等）在 M7 统一实施。② 登录契约直接按后端 `LoginVO` 双 token 结构实现（accessToken + refreshToken），mock 同构返回，联调期 `VITE_USE_MOCK=false` 即切真实 `/api/auth/login-password`，前端零改动。

**新建文件清单**：`src/stores/user.js`、`src/api/request.js`、`src/api/auth.js`、`src/mock/index.js`、`src/mock/user.js`、`src/composables/useAuth.js`、`.env.production`；实装 `LoginView.vue` 与路由守卫。

**验收标准**：

- [ ] 未登录直接访问 /home /checkin /qa /stats 均被重定向到 /login，且地址带 redirect 参数
- [ ] 输入任意非空账密可登录，400ms loading 后跳转 redirect 目标或 /home；登录成功后 localStorage 中同时存在 accessToken 与 refreshToken（双 token 落库正确）
- [ ] 登录后刷新页面，登录态保持（localStorage 持久化）
- [ ] 已登录状态访问 /login，自动回到 /home
- [ ] 手动篡改 localStorage 中 accessToken 模拟过期 → 触发任意接口请求时自动静默刷新并重放，页面无跳转、用户无感知，请求完成后 accessToken 已更新
- [ ] 同时篡改 accessToken 与 refreshToken 模拟双双失效 → 请求后被踢回 /login 并提示「登录已过期」
- [ ] 手动清除 localStorage 中全部 token 后刷新任意需登录页，被踢回 /login
- [ ] NavBar 显示昵称，点击退出后回到 /login 且受控路由再次不可直达（双 token 均被清除）

**预计产出**：完整的登录—守卫—静默刷新—登出闭环；契约与后端 LoginVO 对齐，双端账号互通的联调基础就绪。

### M3 打卡页（功能态）

**目标**：打卡核心数据流跑通（样式从简，套默认 P5Card）。

**新建文件清单**：`src/mock/checkin.js`、`src/api/checkin.js`、`src/stores/checkin.js`、`src/composables/useFormatDate.js`、`src/components/business/CheckinCalendar.vue`、`src/components/business/CheckinForm.vue`；实装 `CheckinView.vue`。

**验收标准**：

- [ ] 进入页面默认显示当月日历，可前后翻月且翻月后红点正确
- [ ] 打卡页容器使用 P5Card.vue 组件（日历卡与表单卡均为切角容器）
- [ ] mock 数据红点符合断点算法：从今天回溯连续 5~7 天有红点，断点日（今天 −5 / −6 / −7 中随机一天）无红点，streakDays 可算出且为 5~7 浮动值，近 7 天打卡数 ≥ 4
- [ ] 点击有红点日期，下方展示该日完整记录（距离 / 时长 / 配速 / 心情 / 备注）
- [ ] 提交合法表单后，当天红点即时出现、详情同步刷新，全程无需刷新页面
- [ ] 距离 / 时长必填且大于 0，配速随输入实时预览，格式如 6'09"/km
- [ ] 90 天记录总量合理（目测 40~60 条），同一天多次进入数据不变

**预计产出**：可真实操作的打卡日历 + 表单；数据来自 mock，但调用链路与真实接口完全一致。

### M4 答疑页（功能态）【v1.2 重构：聊天式】

**目标**：聊天式 AI 问答闭环（RAG-ready 契约，LLM 直连起步）。

**新建文件清单**：`src/mock/qa.js`、`src/api/qa.js`、`src/components/business/QaChatMessage.vue`；实装 `QaView.vue`。

**验收标准**：

- [ ] 聊天流正确渲染：用户消息右侧红底气泡、助手回复左侧切角气泡，新消息自动滚动到底部
- [ ] 提问后助手气泡先显示 loading 动画（mock 延迟 800ms），再渲染 answer 正文
- [ ] mock 命中伤病关键词（如「膝盖疼」）时，回复附带米色 safetyTip 提示条；普通问题不显示
- [ ] 所有助手回复底部渲染 sources 来源标签（mock 返回模型标识 + 模拟文档名各一组验证两种形态）
- [ ] 四分类快捷提问 chips 可点击发起提问，热门问题侧栏点击填入输入框
- [ ] 历史记录分页加载正确（mock 预置 8 条），「加载更多」可回溯，点击条目载入聊天流
- [ ] 👍 / 👎 反馈可点击且状态即时更新（刷新后经 history 仍保持）
- [ ] ask 失败时聊天流插入错误气泡并提供「重试」按钮，控制台无未捕获异常
- [ ] 切换 `VITE_USE_MOCK` 为 false 后请求真实 `/api/qa/ask`（后端可用时），前端零改动即可联通

**预计产出**：与小程序体验对齐的桌面端 AI 答疑对话页；契约按 `{answer, sources, safetyTip}` 实现，后端 RAG 服务化接入时前端无需改动。

### M5 数据统计页

**目标**：可视化图表与周 / 月口径切换。

**新建文件清单**：`src/mock/stats.js`、`src/api/stats.js`、`src/components/business/StatsChart.vue`（内含 ECharts 按需注册封装）；实装 `StatsView.vue`。

**验收标准**：

- [ ] 周视图柱状图渲染本周周一至周日 7 天里程，未过日期为 0，柱高与 mock 数据一致
- [ ] 月视图折线图渲染当月逐日趋势，hover tooltip 显示日期与里程
- [ ] 周 / 月切换图表正确重绘，快速连续切换无残留实例（已 dispose）
- [ ] 顶部汇总（总里程 / 打卡次数 / 连续天数）与图表数据同源一致
- [ ] 浏览器窗口缩放时图表自适应，无变形溢出
- [ ] 构建产物未引入 ECharts 全量包（图表相关体积增量控制在 400KB 内，gzip 前）

**预计产出**：双口径统计页，图表采用红黑主题，与整体风格初步统一。

### M6 首页海报化

**目标**：对照打样图完成品牌首页，确立全站视觉标杆。

**新建文件清单**：`src/assets/fonts/bebas-neue.woff2`、`src/assets/images/runner.png`、`src/assets/images/runner.webp`、`src/components/business/HeroSection.vue`、`src/components/business/FeatureGrid.vue`、`src/components/common/GlitchTitle.vue`、`src/components/common/TapeTag.vue`；实装 `HomeView.vue`（含数据条）。

**验收标准**：

- [ ] Hero 区呈现 RUN WISE 大标题（Bebas Neue + 错位投影）与副标语「跑步训练 · 智能答疑 · 成长记录」
- [ ] 跑者剪影（透明底 WebP）位于 Hero 右侧，边缘无明显白边 / 毛边
- [ ] 红色赛道弧线背景元素呈现，与黑底形成撞色构图
- [ ] 四宫格为切角卡片 + 左上胶带标签 + 右下红色三角角标，四个入口行为与第 4 章定义一致
- [ ] 数据条展示本周里程 / 打卡次数 / 连续天数三组大数字，数据来自 stats mock
- [ ] 与打样图并排对比，配色 / 构图 / 胶带元素还原度达 90% 以上
- [ ] 1280 与 1920 宽度下不破版，900px 以下四宫格折两列

**预计产出**：打样图级还原的品牌首页；三件套组件在此里程碑定型，供 M7 全站复用。

### M7 全站视觉统一 + 联调预留

**目标**：皮肤统一与接口切换就绪。

**新建文件清单**：原则上无新增业务文件——改造既有 views 与 components 样式、补全 `p5-effects.css` 剩余效果类、完善 `.env.production`。

**验收标准**：

- [ ] 全站检索确认：色值 / 字号 / 间距仅存在于 variables.css 与 p5-effects.css，业务代码零写死
- [ ] 登录 / 打卡 / 答疑 / 统计四页全部套用 P5 皮肤（切角容器 + 红黑配色 + Bebas Neue 数字）
- [ ] 各页胶带数量符合第 3 章浓度分级（内容页 ≤2 处）
- [ ] mock 下全流程走通：登录 → 打卡 → 提问 → 看统计，控制台无报错
- [ ] 将 `VITE_USE_MOCK` 置为 false 并把 `VITE_API_BASE` 指向后端地址，仅改 env 即完成切换，业务代码零改动
- [ ] `npm run build` 构建成功，产物本地 preview 正常

**预计产出**：可交付视觉验收的完整站点，以及一处配置切换即可进入联调的就绪状态。

### M9 产品定位修正 + 交互修正【v2.0 新增，当前里程碑】

**目标**：落实「全能训练中枢」定位——三新页上线（骨架态）、小程序引导逻辑清除、答疑交互修正。

**任务清单**：

1. 新增 `mock/plan.js` / `mock/injury.js` 与 `api/plan.js` / `api/injury.js`（签名见 6.3）；
2. 新增路由与页面骨架：`PlanView`（周切换 + 课表表格）、`InjuryView`（四分类 + 文章列表 + 内嵌详情）、`ProfileView`（资料卡 + 徽章区 + 本周摘要 + 设置 / 退出）；
3. NavBar 更新为六项入口（数据统计移出导航，路由保留）；`mock/qa.js` 历史去重（写入时按「日期 + 问题」，预置数据清洗）；
4. 答疑页交互修正：热门问题 / 历史条目 / 空状态点击一律「填入输入框并聚焦」，**删除一切自动发送行为**；分类 chips 改二级候选条设计（含数据注入的个性化问题，见 4.5）；删除 `HomeView` 小程序 toast 与 `FeatureGrid` 的 miniapp 事件，训练计划 / 伤病预防卡改站内路由。

**验收标准**：

- [x] 全站检索「小程序」仅剩登录页绑定引导一处合法文案，功能性引导为 0（已达成，2026-08-31）
- [x] 六项导航可直达 8 条路由（含 /stats 直链），守卫覆盖三新页（已达成，2026-08-31）
- [x] 答疑页任何点击都不会未经确认自动发出提问（已达成，2026-08-31）
- [x] 答疑历史同一条记录不重复出现（连续刷新 + 提问后再核对）（已达成，2026-08-31）
- [x] /plan 周切换可用、/injury 分类与详情可读、/profile 资料与退出可用（已达成，2026-08-31）

### M10 构图改造（不对称拼贴）【v2.0 新增，视觉胜负手】

**目标**：按 3.4 构图法则重排全站，把「AI 味的工整」改为「P5 的拼贴张力」。**只动排布，不加新元素、不动数据流。**

**任务清单**：

1. `variables.css` 增加 `--tilt-1~4 / --tilt-hero` 倾斜令牌（3.4.1）；
2. 首页：入口区改 1 大 + 3 小不对称拼贴（独立旋转 + 错位 8~12px + 重叠 10~20px + hover 露边），Hero 标题越界出血 + 文案区微倾 -2° + 跑者对角构图；
3. 打卡页 7:5、答疑页 8:4 不等分栏；统计页汇总卡大 / 中 / 小三档 + 各自微倾错位；
4. `TapeTag` / `P5Card` 支持位置覆写，全站胶带自由化（跨边界、位置与角度不重复）；
5. 各内容页页头与内容区之间加 clip-path 斜切分隔带（红黑交错 ±12°）；
6. 硬规则落地校验：正文 / 表单控件零倾斜；无横向滚动溢出；重叠不压内容与可点区。

**验收标准**：

- [x] 首页截图与打样图并排：不对称拼贴 + 出血 + 胶带自由度达「放得开」档特征（已达成，2026-08-31，1+3 拼贴 + 独立旋转/错位/重叠 + 胶带四贴法各异 + Hero 出血微倾）
- [x] 三张内容页栅格比实测为 7:5 / 8:4 / 三档宽，无均分栅格残留（已达成，实测 483:345 / 552:276 / 446:372:301）
- [x] 1280 / 1920 宽无破版、无横向滚动条（1220px 环境实测无溢出，全页 scrollWidth ≤ innerWidth）；900px 以下重叠与倾斜自动收敛为堆叠（断点代码就绪，验收环境视口下限 1220px，待真机复核）
- [x] 表单控件与图表坐标区实测零倾斜（已达成，全站 input/select/textarea/button computed transform 均为 none）

### M8 全站视觉精修（皮肤阶段）【v2.0 重定义：排在 M9 / M10 之后】

**目标**：在骨架（M10）与功能（M9）稳定后，叠加 P5 皮肤细节与动效，作为所有非阻塞性样式问题的统一处理窗口。

**范围与规则**：

- **皮肤元素（本阶段新增）**：半调网点（halftone dots）、星形 / 闪电几何飞溅、RUNWISE 水印字、装饰性贴纸划痕，以及页面进场 / 徽章达成等动效；
- 所有**非阻塞性样式问题**（间距、字号、对齐、动效细节、颜色微调等不影响功能验收的视觉项）一律推迟至本阶段统一处理；M9 / M10 开发期间不接受、不处理逐条样式修改请求，样式反馈统一登记到 M8 待办清单；
- 阻塞性问题（布局破版、文字不可读、遮挡交互、数据错误、事实性文案错误）不受此约束，随时修复；
- 已按方案 v2.0 落地的项目（错位投影标题、页头红条斜纹、Bebas 数字、日历菱形标记、ECharts 红黑主题、周口径统一、月视图平滑面积图等）计入已完成项，本期核查回归即可。

**验收标准**：

- [ ] 全站八页视觉走查：三色纪律（#E60012 / #0A0A0A / #F5F0E6）零越界
- [ ] 1280 / 1920 / 900 三档宽度走查，无破版与视觉空洞
- [ ] 网点 / 星形 / 水印仅出现在「全开 / 放得开」浓度档页面，内容页不受污染
- [ ] 全部大数字统一 Bebas Neue + tabular-nums；标题投影为纯色实心（无模糊 / 无半透明残影）
- [ ] M8 待办清单中登记的样式问题逐条闭环或明确降级处理

---

## 9. 风险与注意事项

**① 跑者剪影图的抠图与压缩（WebP）**
素材从打样图裁切跑者区域，用 remove.bg 或 PS「选择主体 + 选择并遮住」抠透明底；注意去除边缘红色溢色，避免深色底上出现白边 / 红边光晕。导出 PNG 后经 Squoosh 转 WebP（有损 quality ≈ 80，宽控制在 800px 内，目标 <100KB），模板中用 `<picture>` 提供 PNG 兜底。直出 PNG 可能超过 500KB，禁止直接上线。若抠图质量不达标，备选方案为购买 / 自绘 SVG 剪影，不阻塞 M6。

**② iOS Safari 上 clip-path 兼容性**
切角与胶带锯齿均依赖 clip-path：统一只使用 `polygon`（百分比 + calc），禁用旧 iOS 不支持的 `clip-path: path()`；构建链补 `-webkit-clip-path` 前缀以覆盖老版本 Safari。提供 `@supports` 降级路径：不支持时退化为直角卡片 + 伪元素红色角标、胶带退化为直边。真机验收至少覆盖 iOS Safari 16 与微信内置浏览器，重点过首页与打卡页。

**③ ECharts 按需引入减小体积**
只从 `echarts/core` 注册 BarChart、LineChart、Grid / Tooltip / Legend 组件与 CanvasRenderer，禁止全量引入（全量约 1MB）。注册逻辑集中在 StatsChart.vue；组件卸载与数据重置时 `dispose()`，防止 iOS WebView 内存泄漏；构建后用可视化体积分析复核图表增量（目标见 M5）。

**④ 字体版权与加载**
Bebas Neue 为 OFL 开源可商用，但必须自托管（Google Fonts 外链有网络可达性风险），只打包 latin 子集 woff2（约 20KB）；中文一律系统字体栈；`font-display: swap` 防止数字区域长时间空白闪烁。

**⑤ mock 与真实接口的一致性**
第 5 章结构定义即前后端契约，后端建表与 VO 字段命名须与其对齐；日期一律 `'YYYY-MM-DD'` 字符串，避免时区偏移；api 函数签名是联调边界，切换 mock 不允许改动组件层与 store 层代码。

**⑥ 高对比海报风的可读性**
红色仅用于强调（标题投影 / 按钮 / 角标 / 激活态），正文一律白 / 米色，满足 WCAG AA 对比度；深红 hover 态需与主红拉开明度；大面积红黑渐变在低品质屏幕易出现色带，可叠加细噪点纹理 CSS 缓解。

**⑦ token 存储安全**
开发期 token 存 localStorage 可接受（存在 XSS 风险）；token 读写集中收口在 `stores/user.js` 与 `request.js` 两处，后端若提供 httpOnly Cookie 方案，仅需改这两处即可迁移，不动业务代码。

**⑧ AI 答疑链路的降级与演进（v1.2）**
当前后端为 LLM 直连（DashScope，30s 同步超时），RAG 服务化后链路变长（检索 + 生成），需约定：RAG 服务异常时自动降级 LLM 直连，响应结构 `{answer, sources, safetyTip}` 两种模式下完全一致，前端按同一契约渲染；ask 为同步接口，前端 loading 态必须有超时兜底（超过 30s 提示重试）；SSE 流式输出作为后续增强，届时 api 层新增 `askQuestionStream` 而非改动现有签名，聊天组件按特性开关渐进切换。

**⑨ 登录体系与双端互通（v1.3，联调前需后端完成）**
双端方案为「账号统一、方式各宜」：小程序保持微信静默登录（openid 体系，零改动），Web 端账密登录，用户在小程序「我的 → 设置账密」后 `openid` 与 `username` 绑定同一条 User 记录，两端以同一 `userId` 读写打卡 / 答疑 / 统计数据，天然互通。联调前需后端完成 4 项小改动（均不阻塞前端 mock 开发）：

| #   | 改动                                                                                                   | 目的                                                                                                                                                                |
| --- | ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `User` 表增加 `username`（唯一索引）、`password`（BCrypt）两列                                         | Web 账密登录的账号载体，openid 登录路径零改动                                                                                                                       |
| 2   | 新增 `POST /api/auth/login-password`                                                                   | Web 登录接口，返回现有 `LoginVO` 双 token 结构                                                                                                                      |
| 3   | 新增 `POST /api/user/set-password`                                                                     | 小程序内登录态下设置 / 修改账密（绑定动作）                                                                                                                         |
| 4   | JWT claims 增加 `device` 字段，Redis key 改为 `runwise:token:{userId}:{device}:access`（refresh 同理） | **双端同时在线**：当前实现为单 key 覆盖式（`JwtInterceptor` 严格比对 Redis 值），后登录端会挤掉先登录端且 refreshToken 一并失效；加设备维度后两端独立刷新、独立登出 |

边界说明：从未设置账密的纯微信用户无法登录 Web 端（无跨端凭证），属合理限制，登录页给引导文案即可；「小程序扫码登录 Web」需微信开放平台企业资质（网站应用），个人开发者不可行，列为远期可选增强。前端侧对应动作已前置到 M2：双 token 契约 + 静默刷新队列，联调时 `VITE_USE_MOCK=false` 即切真实接口，零改动。
